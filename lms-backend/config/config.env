# lms-backend/config/config.env
NODE_ENV=development
PORT=5001

# Change these values to match the new PostgreSQL user and database you created
# The database will be named `lms`
# The user will be `lms_user`
# The password will be `Nexfern_Academy`

# The DATABASE_URL is an alternative format, but the application seems to use the individual variables.
# You can leave this commented out or update it for future use.
# DATABASE_URL=postgresql://lms_user:Nexfern_Academy@localhost:5432/lms

DB_USERNAME=lms_user
DB_PASSWORD=Nexfern_Academy
DB_NAME=lms
DB_HOST=127.0.0.1

JWT_SECRET=supersecretjwtkey
JWT_EXPIRE=30d # Token expiry time, e.g., 30 days
JWT_COOKIE_EXPIRE=30 # Cookie expiry time in days
MAX_FILE_UPLOAD=10000000 # Max file size for uploads (e.g., 10MB)